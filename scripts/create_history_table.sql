CREATE TABLE `WideWorldImporters`.`LoadHistory`
(
  TableName STRING,
  LoadDate DATETIME,
  Status STRING,
  Details STRING
);

CREATE TABLE `WideWorldImportersDW`.`WarehouseHistory`
(
  TableName STRING,
  LoadDate DATETIME,
  Status STRING,
  Details STRING
);
